# Codex 工作指引（每次会话自动加载）

本仓库唯一开发入口文档：`docs/codex/2026-07-15-codex-development-handbook.md`。

每次会话开始：

1. 完整阅读上述总纲，重点：第 8 章红线、第 9 章任务卡、第 2.4 节开放问题。
2. 若用户未指定任务卡：先报告当前进度（`git log --oneline -10` + 总纲第 13 章维护表 + `dotnet test` 是否全绿），再询问执行哪张卡。
3. 一次只做一张任务卡。开工前列出将修改的文件、依赖和验收命令；先写失败测试，再做最小实现；完成后运行测试并报告真实输出。
4. 每完成一张任务卡执行一次 `git commit`（提交信息含卡号，如 `CX-404: ...`）；每完成一个里程碑更新总纲第 13 章维护表并 push。
5. 没有对应任务卡的新需求：先按总纲第 9 章格式起草新任务卡（编号顺延，如 CX-6xx）追加到总纲，经人工确认后再动代码。
6. 涉及剧情文本、台词、视觉风格、付费服务开通或总纲 2.4 开放问题：停下请求人工决策，不要自行选择。
