# CX-507 缺口记录（已解决）

日期：2026-07-25  
最终采集结果：`1125` 项 reference-only 素材，计划总量下限为 `800`；覆盖率为 `1125/800`，逐项目标 `0` 个失败。

本记录保留作为恢复采集的审计轨迹。用户已确认素材仅用于个人游戏和观看，允许 `unverified` 来源以及短缺目标的较低分辨率例外。所有未验证或短缺资产均标记 `needs_review`，商业使用/修改字段为 `false`，且不进入 Unity 构建。

最终校验：

- `validate-plan`：通过（34 个省级地区，800 项总量下限）
- 目录 Schema、文件长度、SHA-256 与许可策略校验：通过
- 覆盖率：通过，`failedTargetIds=[]`
- ReferenceLibrary 专项测试：`87/87` 通过
- Video.sln：`850` 通过、`1` 个既有 FFmpeg 集成测试按默认配置跳过

恢复过程使用目标/查询分批筛选；临淄古都额外加入山东区域建筑回退证据，最终补齐临淄 `10/10` 与火山 `8/8`。
